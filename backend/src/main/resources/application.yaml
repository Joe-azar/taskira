spring:
  application:
    name: taskira-api

  datasource:
    url: jdbc:postgresql://localhost:5432/taskira
    username: taskira
    password: taskira
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

app:
  jwt:
    secret: taskira-super-secret-key-change-me-at-least-32-characters
    expiration-ms: 86400000