/**
 * Named interface: audit's enums (entity type, action), referenced by every module that
 * calls AuditService.record(...) to describe what happened.
 */
@org.springframework.modulith.NamedInterface
package com.joe.taskira.audit.enums;
