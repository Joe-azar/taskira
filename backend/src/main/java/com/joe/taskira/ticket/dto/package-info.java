/**
 * Named interface: Ticket's response DTOs. The dashboard module reuses
 * TicketSummaryResponse directly for its aggregate view instead of duplicating it.
 */
@org.springframework.modulith.NamedInterface
package com.joe.taskira.ticket.dto;
