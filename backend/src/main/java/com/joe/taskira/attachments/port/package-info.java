/**
 * Named interface: the DocumentStorage port, reused by exports for the bulk export
 * workbook - the same generic "store bytes under a server-generated key" contract an
 * attachment already needs, not attachment-specific behavior.
 */
@org.springframework.modulith.NamedInterface
package com.joe.taskira.attachments.port;
